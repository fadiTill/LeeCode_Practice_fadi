def sorted_squares(nums)
    new_array = []
# create  a new array 
 nums.each {|number|   new_array << number**2}
 #loop and push in the new array square value 
 new_array.sort{ |a,b| a <=> b}
 #return sorted array
 

 
end